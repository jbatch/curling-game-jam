'use strict';

import 'phaser';

export default class GameScene extends Phaser.Scene {
 

  constructor() {
    super('GameScene');
  }

  init() {}

  preload() {
   
  }

  create() {
   
  }

  

  update(time, delta) {
  }
}
